#include<stdio.h>               /*一个简单的C程序*/
int main(void)
{
    int num;                    /*定义一个名为num的变量*/
    num = 1;                    /*为 num 赋一个值*/

    printf("I an a simple ");    /*使用 printf() 函数*/
    printf("computer.\n");
    printf("My favourate number is %d because it is first.\n",num);

    return 0;
}